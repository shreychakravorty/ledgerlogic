import React,{useState} from 'react';
import { Headphones, ArrowRight, RotateCcw } from 'lucide-react';
import { supportCases } from '@/components/accounting/supportData';
import SupportConversation from '@/components/accounting/SupportConversation';
export default function Support(){
 const [active,setActive]=useState(null),[run,setRun]=useState(0);
 return <div className="page-enter mx-auto max-w-5xl"><p className="eyebrow">THINK LIKE A SUPPORT CONSULTANT</p><h1 className="page-title mt-3">The customer called.</h1><p className="muted mt-3">Listen for the real problem. Follow the evidence. Explain the fix.</p><div className="mt-8 grid items-start gap-6 lg:grid-cols-[280px_minmax(0,1fr)]"><div className="space-y-3">{supportCases.map(s=><button key={s.id} onClick={()=>setActive(s)} className={`panel w-full p-5 text-left ${active?.id===s.id?'!border-[#b8a0d0] !bg-[#f4eff9]':'hover:border-[#d3c0e1]'}`}><div className="flex items-center justify-between text-[#ae94c4]"><Headphones size={20}/><ArrowRight size={15}/></div><p className="my-3 text-sm font-bold leading-relaxed">{s.title}</p><p className="text-[10px] text-[#a391b0]">{s.topic} · 4–6 min</p></button>)}{active&&<button className="secondary-btn w-full" onClick={()=>setRun(run+1)}><RotateCcw size={14}/>Restart this case</button>}</div>{active?<SupportConversation key={`${active.id}-${run}`} scenario={active}/>:<div className="panel grid min-h-[380px] place-items-center p-8 text-center"><div><span className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-full bg-[#eee6f6] text-[#aa8ec1]"><Headphones size={30}/></span><h2 className="text-xl font-bold">Your customer needs an accounting brain.</h2><p className="muted mx-auto mt-3 max-w-xs">Pick a call. Ask questions to uncover clues, diagnose the root cause, and explain the correction in plain language.</p><button onClick={()=>setActive(supportCases[0])} className="primary-btn mt-6">Take your first call <ArrowRight size={14}/></button></div></div>}</div></div>;
}